# Implementation Strategy

- Use a fresh isolated worktree from the current detached HEAD.
- Keep the change to README badge metadata and session docs.
- Leave the canonical detached/rebase state untouched.
- Record final integration as prepared evidence, not canonical fast-forward.
