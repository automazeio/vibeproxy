# Research

- Canonical VibeProxy was clean but detached at `1308c6c` with branch output
  `HEAD (no branch)`.
- Existing `vibeproxy-wtrees/sladge-badge` evidence was stale
  (`ahead 2, behind 16`) and dirty due an unrelated workflow modification.
- README directly describes AI subscription routing for Claude Code, ChatGPT,
  Gemini, Qwen, Antigravity, Z.AI GLM, Vercel AI Gateway, and AI coding tools.
- A fresh isolated worktree was required to avoid disturbing the detached
  canonical checkout.
